import {NavLink,Outlet} from 'react-router-dom';import {cloudEnabled} from '../lib/supabase';import {useEffect,useState} from 'react'
const links=[['/','Dashboard'],['/inventory','Inventory'],['/purchases','Purchases'],['/sales','Sales & Billing'],['/products','Products'],['/customers','Customers'],['/reports','Reports'],['/settings','Settings']]
export function Layout(){const[online,setOnline]=useState(navigator.onLine);useEffect(()=>{const a=()=>setOnline(true),b=()=>setOnline(false);addEventListener('online',a);addEventListener('offline',b);return()=>{removeEventListener('online',a);removeEventListener('offline',b)}},[]);return <div className="app-shell"><aside className="sidebar"><div className="brand"><div className="brand-mark">RM</div><div><strong>RythuMithra</strong><span>Agri ERP</span></div></div><div className="shop-caption">FERTILIZERS • PESTICIDES • SEEDS</div><nav>{links.map(([to,label])=><NavLink key={to} to={to} end={to==='/' } className={({isActive})=>isActive?'nav-link active':'nav-link'}><span>{label}</span></NavLink>)}</nav><div className="sync-card"><span className={online?'dot online':'dot'}></span><div><strong>{online?'Online':'Offline'}</strong><small>{cloudEnabled?'Cloud sync configured':'Local offline mode — configure Supabase for sync'}</small></div></div></aside><main className="main"><header className="topbar"><div><div className="eyebrow">RYTHUMITHRA FERTILIZERS • PESTICIDES • SEEDS</div><h1>Inventory & Billing</h1></div><span className="pill">{online?'● Connected':'● Offline'}</span></header><section className="content"><Outlet/></section></main></div>}
