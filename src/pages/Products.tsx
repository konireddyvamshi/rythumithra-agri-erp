import {FormEvent,useEffect,useState} from 'react';import {db} from '../lib/db';import {saveProduct} from '../lib/services';import type {Product} from '../types'
export function Products(){const[rows,setRows]=useState<Product[]>([]),[q,setQ]=useState(''),[open,setOpen]=useState(false),[name,setName]=useState(''),[hsn,setHsn]=useState(''),[pack,setPack]=useState(''),[unit,setUnit]=useState('Unit'),[reorder,setReorder]=useState('0'),[msg,setMsg]=useState('');const load=()=>db.products.orderBy('name').toArray().then(setRows);useEffect(()=>{load()},[]);const submit=async(e:FormEvent)=>{e.preventDefault();try{await saveProduct({name:name.trim(),hsnCode:hsn.trim(),packSize:pack,unit,reorderLevel:Number(reorder)||0});setName('');setHsn('');setPack('');setReorder('0');setOpen(false);setMsg('Product created.');load()}catch(e:any){setMsg(e.message)}};const filtered=rows.filter(x=>(x.name+' '+x.hsnCode).toLowerCase().includes(q.toLowerCase()));return <><div className="page-head"><div><h2>Product Master</h2><p>Standardize product names; batches carry cost, MRP, selling price, expiry and GST.</p></div><button className="button primary" onClick={()=>setOpen(!open)}>+ New product</button></div>{open&&<form className="panel" onSubmit={submit}><div className="form-grid four"><label>Product name<input required value={name} onChange={e=>setName(e.target.value)}/></label><label>HSN code<input required value={hsn} onChange={e=>setHsn(e.target.value)}/></label><label>Pack size<input value={pack} onChange={e=>setPack(e.target.value)} placeholder="100 g / 250 ml"/></label><label>Unit<input value={unit} onChange={e=>setUnit(e.target.value)}/></label><label>Reorder level<input type="number" min="0" value={reorder} onChange={e=>setReorder(e.target.value)}/></label></div><div className="form-actions"><span className="message">{msg}</span><button className="button primary">Save product</button></div></form>}<div className="toolbar"><input placeholder="Search product or HSN" value={q} onChange={e=>setQ(e.target.value)}/></div><div className="table-wrap"><table><thead><tr><th>Product</th><th>HSN</th><th>Pack</th><th>Unit</th><th>Reorder</th></tr></thead><tbody>{filtered.map(p=><tr key={p.id}><td><strong>{p.name}</strong></td><td>{p.hsnCode}</td><td>{p.packSize||'—'}</td><td>{p.unit}</td><td>{p.reorderLevel}</td></tr>)}{!filtered.length&&<tr><td colSpan={5} className="empty">No products yet.</td></tr>}</tbody></table></div></>}
